//
//  ContentView.swift
//  Landmarks
//
//  Created by Omer on 1/30/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("한국 사람 입니다.")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
